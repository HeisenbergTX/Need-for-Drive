import React from "react";
import style from "./Language.module.css";

export const Language = () => {
  return (
    <section className={style.section}>
      <button className={style.languageBtn}>Eng</button>
    </section>
  );
};
