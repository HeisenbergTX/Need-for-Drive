import React from "react";
import s from "./Language.module.css";

export const Language = () => {
  return (
    <section className={s.section}>
      <button className={s.languageBtn}>Eng</button>
    </section>
  );
};
